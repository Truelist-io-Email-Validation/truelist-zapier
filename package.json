{
  "name": "truelist-zapier",
  "version": "0.1.0",
  "description": "Zapier integration for Truelist email validation",
  "license": "MIT",
  "main": "dist/index.js",
  "dependencies": {
    "zapier-platform-core": "15.x"
  },
  "devDependencies": {
    "typescript": "^5.0.0",
    "zapier-platform-cli": "15.x",
    "@types/node": "^18.0.0",
    "jest": "^29.0.0",
    "ts-jest": "^29.0.0",
    "@types/jest": "^29.0.0"
  },
  "scripts": {
    "build": "tsc",
    "test": "jest --testPathPattern='src/test'",
    "lint": "tsc --noEmit"
  },
  "engines": {
    "node": ">=18.0.0"
  }
}
